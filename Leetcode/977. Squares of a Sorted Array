977. Squares of a Sorted Array

class Solution {
    public int[] sortedSquares(int[] nums) {
        int ans[]=new int[nums.length];
        int j=0;
        for(int n:nums){
            ans[j++]=n*n;
        }
        Arrays.sort(ans);
        return ans;
        
        
    }
}
